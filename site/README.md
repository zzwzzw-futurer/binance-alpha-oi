# Binance Alpha OI Monitor Site

Static dashboard for the Binance Alpha futures monitor.

The page reads:

- `data/latest.json`
- `data/history.json`

These files are generated by the local monitor script.
